# Embedding Service

This service provides text embedding using OPEA's embedding components.

## Running

Use OPEA's `comps/embeddings` to start the container.

## Endpoint

- Default: `http://localhost:8009/v1/embeddings`

## Environment Variables

- `EMBEDDING_ENDPOINT`: Embedding service endpoint
